---
import "../styles/hex-container.css"

interface Props {
    hexHeight: string,
    margin: string,
    width: string,
    height?: string,
    extraStyles?: string,
};

const {
    hexHeight,
    margin,
    width,
    height = "fit-content",
    extraStyles = "",
} = Astro.props;
---
<div
    class="hex-container"
    style={`height: ${height}; width: ${width}; --hex-height: ${hexHeight}; --margin: ${margin}; ${extraStyles}`}
>
    <div class="hex-grid">
        <slot />
    </div>
</div>
