---
import "../styles/hex-container.css"

interface Props {
    style?: string,
};

const { style = "height: fit-content; width: 100%;" } = Astro.props;
---
<div class="hex-container" style={style}>
    <div class="hex-grid">
        <slot />
    </div>
</div>
